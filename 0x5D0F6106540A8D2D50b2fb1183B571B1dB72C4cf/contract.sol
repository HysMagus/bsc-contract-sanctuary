/*
  $$$$$$$$$$$$$$$$$$$$$$$$$$$VVVVVVVVVVVVVV$$$$$$$VVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIIIFFFFFFFFFF*******************::::::::::::::::::::::::::::::****FF**
$$$$$$$$$$$$$$$$$$$$$$$VVVVVVVVVVVVVVVVVVVIIIIIIIIIIIIFFFFFFFFFFFFFFF*****************************************************************************FF**
VVVVVVVVVVVVIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF***********************************************************************************
IIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF****************************FF**************************************************
FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF****************************
FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF***********************************
FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF*****FFFFFFFFFFFFFFFFFFFFFFFFFFFFF*******************************************
****FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF*********FFFFFFFFFFFFFFFFFFFFFFFFFFF*F******************************************
******FF********FFF*FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF***********FFFFFFFFFFFFFFFFFFFFFFFFFFF*******************************************
**********************FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF*************FFFFFFFFFFFFFFFFFFFFFFFFF*********************************************
**************************FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF***************FFFFFFFFFFFFFFFFFFFFFFFFFF*******************************************
********************::********FFFFFFFFFF********************FFFF**********F*****FFFFFFFFFFFFFFFFFFFFFFFFFFFFF*****************************************
******************::::::*:******FFFF************************F***********FF******FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
*****************:::***:*:::***************************************:**********:*IIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFF
**********FFFFF**::::******::******F*******************************:********::*IIVVVIIIIIIIIIIIFFFFFFFIIFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
FFFFFFFFFFFFFFFF**::::********:*********************************:***:********:*IIIIIIIIIIIIIFF********FFIIIIIIIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
IIIIIIIIIIIIIIIII*:::::::**F**:**********************************::*::********FIVVVVVVIIIIFF***::::::::**FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFIIIIII
IIIIIIIIIIIIIIIIII*:::::::*F*****************************::::::****:**********FIIIIIIIIIIF***::::::::::::*FIIIIIIIIIIIIIIIIIIIIIIIIIIIIFIIIIIIIIIIIIII
VVVVVVVVVVVVVVVVVVIF*::*::*IIFF***********:***********::::::::::::***:******FVVVVVVVVVVVIF*************:::*FIIIIIIIIIIIIIIIIIIFFFFFFFFFFFIIIIIIIIIIIII
IIIIIIIIIIIIIIIIIIFFF*::**:***********:::**::********:::::::::::::::**::****IVVVVVVVVVVIF*******************FIIIIIIIIIIIIIIIIIIIIFFFFFFFFIIIIIIIIIIIII
IIVVVVVVVVVVVVVVVVVVIF**********FF*****:**:::::****:::::::::::::::*:***::::*VVVVVVVVVVVIF*********FFIFFF**::*FIIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
VVVVVVVVVVVVVVVVVVVVVIF*******FFFF*********:**::*****************::*::**:::FVVVVVVVVVVIF********FFVVVIFF**::*FIIIIIIIIIIIIFFFFFIFFFFFFFFFFFFFFIIFFFFFI
VVVVVVVVVVVVVVVVVVVVVVVVFFFFFFIFFF******IF***************FVVF*::::::::::::*IVVVVVVVVVVVIFFFFFF**FVVVFF****::*FIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII
VVVVVVVVVVVVVVVVVVVVVVVVVIFIIIIIIIF***V$$$VIF****************:::::::::::::*IVIFFFFFIIIVIFFFFFFFFFFFFFF****:*FVVVVVVVVIIIIIVIIIIIIIIIIIIIIIIIIIIIIIIIII
VVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIFFIF**:**FVVIFF****************::::::::::::*FFF********FFIIFFIIFFFFFFF******FIVVVVVVVVVVVVVVVVIVVVVIIIIIIIIIIIIIIIIIIII
VVVVVVVVVVVVVVVVVVVVVVVVVVIFFFF**IF******FIFF***F*************::::::::::::**FFF**********FIIIIIIIIVVF******IVVVVVVVVVVVVVVVVVVVVVVVVVVIIVVVVVVVVVVVVVI
VVVVV$$$$$VVVVVVVVVVVVVVVVVFFF***FFF****FVIF****FF********::**::::.:::::::*FFFFF************FFIIIIVVVIF***FIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
$$$$$MMMMMM$$VVVVVVVVVVVVVVIFF***FFF***FIIFF********FF***:::::::...:::::::***FFF***************FIIVVVVIF****IVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
MMMMMMMMMMMM$$VVVVVVVVVVVVVVFFF**FFF**FFIIF******FFFVIIF**:::::::::::::::*************************FIIIII**:*FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
MNMMMMMMMMMMM$$VVVVVVVVVVVVVVIFFFIIF**FIIF*****FIVV$$VVVVV*::::::::..::::**:******************************::*IVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NMMNNMNMMMMMMM$$$$$$$VVVVV$$$VVFFIIIFFFFIF**:*$$$$$VVVV$$$F:::::::::::::****::*************************:::::*FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNMNNNMMMMM$$$$$$$V$$$$$$$$VIFIIFFFFFF**:*$$$$MM$$$$VI*:::::*::::::******::*************************:::::*IVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNNNNNNNNNNMMM$$$$$$MMMMMM$VIFFFIFIFFF**::*IV$$$$$$$VI*:::*IF::::::*******::***********************::::::*FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNNNNNNNNNNNNNMMMMMMMMMNMM$VFIIIFFFIIFF******FIVVVVIF****IIF*:::::****::::::**********************:::::::*FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNNNNNNNNNNNNNNNNMMMNNNNM$VIIIIVIFFFFFIIF********FIFFIIIVVI*::*:::****::::::**************:::****:::::::::FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNNNNNNNNNNNNNNNNNNNNNNN$VIIIIVVIIIFFFIV$$$$VVVVVVIFIFF*FF*:::*::******::::::************:::****:::::::::*FVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
NNNNNNNNNNNNNNNNNNNNNNNNNNMMVIIIVVIIIIIIIFFFFVVIF*FFF******:*::::::::*****::::::::**********::::****:::::::::*IVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV$$$MM
NNNNNNNNNNNNNNNNNNNNNNNNNM$VIIIVVIIIIIFFFFFF***F****:::::*::::::::::****:::::::::**********:::::*****::::::::*VVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV$$$$MMMMM
NNNNNNNNNNNNNNNNNNNNNNNMM$FFFFIIVIIIFFFFF*****::::::::::::::::::::*****:::::::::**********::::::*****:::::::*FVVVVVVVVVVVVVVVVVVVVVVVVVVV$$$MMMMMMMMMM
NNNNNNNNNNNNNNNNNNNNNNMMVFFFFFFFIIIFFFF****::::::::::::::::::::::*****::::::::::**********::::::*****:::::::*IVVVVVVVVVVVVVVVVVVVVVV$$$$MMMMMMMMMMMMMM
NNNNNNNNNNNNNNNNNNNNNM$V*******************:::::::::::::::::::::****:::::::::::**********::::::::***:::::::*IVVVVVVVVVVVVVVVVVVV$$$$MMMMMMMMMMMMMMMMMM
MMNNNNNNNNNNNNNNNNNNM$I**::::::::::::::****::::::::::::::::::::***::::::::::::***::::::****:::::::**::::::*FVVVVVVVVVVVVVVV$$$$MMMMMMMMMMMMMMMMMMMMMMM
VV$$MMNNNNNNNNNNNNM$IF**:::::::::::::::::****:::::::::::::::::::::::::::::::::::::::::***II*::::::::::::::*IVVVVVVVVVV$$$$MMMMMMMMMMMMMMMMMMMMMMMMMMMM
VVVVVV$$MMNNNNNNMM$I***::::::::::::::::::::**:::::::::::::::::::::::::::::::::::::::***FVVVF::::::::::::::*IVVVV$$$$$$MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM
$$$$VVVVVV$MMNNMMVF****:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::**IVVVVF::::::::::::::*V$$$$$$MMMMMMMMMMMMMMMMM$$$$$$$$$$$$$$$VVVV
MM$$$$$$VVVVV$$$I******::::::::::::::::::::::::::::::::::::::::::::::::::::::**:::::*FVVVVVF*:::::::::::**I$$$$$$$$$$$$$$$$$VVVVVVVVIIIIIIIIIIIIIIIIFF
MMMMM$$$$$$$$VF*********::::::::::::::::::::::::::::::::::::::::::::::::::::::****:*:*FV$$VI********FFFFFIIVVVIIIIIIIIIIIIIIIIFFFFFFFFFFFFFF**********
$MMMMMMMMMMMV*******::::::::::::::::::::::::**:::::::::::::::::::::::::::************:**IVVVVVIIIIIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFFFIIIIIIIIIIVVVVVVVV
$$$$$MNNNMMI*********:::::::::::::::******************::****::******::****************:*FIIIIIIIIIIIIIIIIIIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
$$$$$MMMM$I***********:::::::::::**FIIIVIF**************FF*******************************VVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVV
VVV$$$$$$I***********::::::::::*FIV$$$$$VVVIIIF**FIIIIVVVVVIFFF*********FFFF*************IVV$$$$$$$$$$VVVVVVVVVVVVVIVV$VVVVVVVVVVVVVVVVVVVVVVVVVVVVIIV
VVV$$$$VF***********::::::::**IVVVVVVVVVVVVVVVVVVVVVV$$MMMMMMMM$VFF*****FFFFF************FVVVVVVVVVVVVVVVVVVVVVVV$$$MMMM$VVVVVVVVVVVVVVVVVVVVVVIIIIIII
VVVVVVVF************::::::*IV$$$$$$$$$$$$$$$$$$$$$$$$MMMMMMMMMMM$V************************IVVVVVVVVVVVVVVVVVVVVVVVMMMMMMM$VVIVVVVVVVVVVVVVVIIIIIIIIIII
VVVVVVF************:::::::I$$$$$$$$$$$$$$$$$$$$$$$$$MMMMMMMMMM$VF*********************:****IVVVVVVVVVVVVVVVVVVVVVV$MMMMMNMVVIIVVVVVVIIIIIIIIIVVIIIIIII
VVVVVIF***********:::::::*V$$$$$$$$$$$$$$$$$$$$$$$$$MMMMMMM$VIF**********************:::***FVVVVVVVVVVVVVVVVVVVVVVV$MMMMMMMVIIIVVVIIIIIIIIIIIV$VIIIIII
VVVVIF************::::::*IV$$$$$$$$$$$$$$$$$$$$$$$$$MMMM$VFF****FF***************:::::::::*FV$VVVVVVVVVVVVVVVVVVVVVV$MMMMMMMVIIIIIIIIIIIIIIIIIV$VVIIII
VVVIF************::::::*VVV$$$$$$$$$$$$$$$$$$$$$$$$$$$$VFF*F*FFFF**************:::::::::**IVV$$VVVVVVVVVVVVVVVVVVVVVV$MMMMMMVVVVVIIIIIIIIIIIIIIVVVVVVI
VVFF*************::::FVVVVVVV$$$$$$$$$$$$$$$$$$$$$$$$VF*********************:::::::::*IVVVVVVV$$VVVVVVVVVVVVVVVVVVVVVVVV$$VVVVVVVIIIIIIIIIIIIIIIIVVVVV
VF**************:::*VVVVVVVVVVVV$$$$V$$$$$$$$$$$$$$VF*********************::::::::**FVVVVVVVVV$$VVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIIIIIIIIIIIIIIIII
F***************:*VVVVVVVVVVVVVVVVVVVV$$$$$$$$$$$VFF*****************:::**::::**FIVVVVVVVVVVVVV$$VVVVVVVVVVVVVVVVIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII
****************IVVVVVVVVVVVVVVVVVVVVV$$$$$$$$$$IF****************:::::::***FIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII
***************FVVVVVVVVVVVVVVVVVVVVVV$$$$$$$$VFF********************:**FIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII
***************V$VVVVVVVVVVVVVVVVVVVVV$$$$$$$VF*******************FFIIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIIIIIIIFFFFFFFFFFFFFFFFFFFFFFF
**************F$VVVVVVVVVVVVVVVVVVVVVV$$$$$VF*******************FVV$$VVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVIIIIIIIFFFFFFFFFFFFFFFIIIIIIIIIIIIVVVVVVVVVVVVVV
*************F$$$VVVVVVVVVVVVVVVVVVVVV$$$VF*******************FIV$$$VVVVVVVVVVVVVVVIIIIIIIIIIIIIIIIIIIIIIVVVVVVVVV$$$VVVFF******FF*::::*****FFIV$VV$$$
************V$$$$VVVVVVVVVVVVVVVVVVVVV$VI********************IVVVVVVVVVVVVIVVVIVVVVVVVVVVVVV$$$VVV$$$MMMMMMMMMMMM$$VIF:::::::::::***::::::::::****VV$$
*********:*F$$$VVVVVVVVVVVVVVVVVVVVVV$$VF***************::**VVVVV$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$MMMMMMMMMMMMVIFF::::::::::::::::::::::::...:::*V$$
******:::::IVVVVVVVVVVVVVVVVVVVVVV$$MMI**************::::*I$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$MMMMMMMMMMVF**::::**F*F**FV$$$$I*:*****::::::V$$
******::::FV$$$$$$$$$$$$$$$$$MMMMMMMMF**************::::*I$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$VV$$MMMM$$MMV::**F***:***::*V$$F:..........:*V$$$
*******::FMMMMMMMMMMMMMMMMMMMMMMMMMMV****************::*$MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM$V$$M$I***********:::F*:::*FIIIFF***I$$$$
*****:::IMMMMMMMMMMMMMMMMMMMMMMMMMMMIF************::::*V$MMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMMM$$VV$$VIFFF****IVFFF**FF**$$$$$$VV$$$$$$
******:FMMMMMMMMMMMMMMMMMMMMMMMMMMMMV**:::::*****:::::V$MMMMMMMMMM$$M$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$MMMMMM$$$$$$$$VVVVIFFV$*::::::*V$$$$$$$$$$$$$M
*FIF**IMMMMMMMMMMMMMMMMMMMMMMM$$MMMMV*:::::::*FF****IV$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$VVVV$$V*******$$$$$$MM$$$$$$$
:*VVV$MMMM$$$$$$$$$$$$$$$$$$$$$$$$$$V*:*::::::****V$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$V$$$******F$$$MMMMMMM$$$$$
FI$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$I***::::**IFI$$$$$$$$$$$$$$$V$$VVVVVVVVVVVVVVIIIIIIIIIIIIIIIIVVV$$$$$$$$$$$$$$$$$$$$$$$$V$$F*****I$$MMMMMMMM$$$$$
IIIIVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVVI*::*FIIIIIIIIIIIIIIIIIIIIIIVVIFFFFFFF****************************FIV$$$$$$$$$$$$$$$MMM$$$V*****V$$MMMMMMM$$$$$$
...............:::::::::::::::::::::::::::::::::::::::::::::::::::::.............................:::::****:**FV$$$$$$$$$$$$MM$$$$F****$$MMMMMM$$$$$$$V
.........................................................................................................::***:**I$$$$$$$$$$$M$$$V***F$$MMM$$$$$$$$VIF
.............................................................................................................::*****IV$$$$$$$$$$$$***I$$$$$$$$$$$VIIF*
................................................................................................................::****FV$$$$$$$$$$F**V$$$$$$$$$VVIF*::
..............................................................................................................:::..:*****FV$$$$$$$V**$$$$$$$$VVVI*::::

 
  Telegram: https://t.me/FrunkpuppyFOMO
  ________________________
*/

 
// SPDX-License-Identifier: MIT
pragma solidity 0.5.17;

// Part: IERC20

/**
 * @title ERC20 interface
 * @dev see https://github.com/ethereum/EIPs/issues/20
 */
interface IERC20 {
  function totalSupply() external view returns (uint256);

  function balanceOf(address who) external view returns (uint256);

  function allowance(address owner, address spender)
    external view returns (uint256);

  function transfer(address to, uint256 value) external returns (bool);

  function approve(address spender, uint256 value)
    external returns (bool);

  function transferFrom(address from, address to, uint256 value)
    external returns (bool);

  event Transfer(
    address indexed from,
    address indexed to,
    uint256 value
  );

  event Approval(
    address indexed owner,
    address indexed spender,
    uint256 value
  );
}

contract Context {
    address Owner;
    constructor () public { Owner = _msgSender(); }
    
    function _msgSender() internal view returns (address payable) {
        return msg.sender;
    }
    
    modifier OnlyOwner() {
        require(Owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }
 
    function _msgData() internal view returns (bytes memory) {
        this; // silence state mutability warning without generating bytecode - see https://github.com/ethereum/solidity/issues/2691
        return msg.data;
    }
}

// Part: ILP

interface ILP {
    function sync() external;
}

// Part: IUniswapV2Factory

interface IUniswapV2Factory {
    event PairCreated(address indexed token0, address indexed token1, address pair, uint);

    function feeTo() external view returns (address);
    function feeToSetter() external view returns (address);

    function getPair(address tokenA, address tokenB) external view returns (address pair);
    function allPairs(uint) external view returns (address pair);
    function allPairsLength() external view returns (uint);

    function createPair(address tokenA, address tokenB) external returns (address pair);

    function setFeeTo(address) external;
    function setFeeToSetter(address) external;
}

// Part: IUniswapV2Pair

interface IUniswapV2Pair {
    event Approval(address indexed owner, address indexed spender, uint value);
    event Transfer(address indexed from, address indexed to, uint value);

    function name() external pure returns (string memory);
    function symbol() external pure returns (string memory);
    function decimals() external pure returns (uint8);
    function totalSupply() external view returns (uint);
    function balanceOf(address owner) external view returns (uint);
    function allowance(address owner, address spender) external view returns (uint);

    function approve(address spender, uint value) external returns (bool);
    function transfer(address to, uint value) external returns (bool);
    function transferFrom(address from, address to, uint value) external returns (bool);

    function DOMAIN_SEPARATOR() external view returns (bytes32);
    function PERMIT_TYPEHASH() external pure returns (bytes32);
    function nonces(address owner) external view returns (uint);

    function permit(address owner, address spender, uint value, uint deadline, uint8 v, bytes32 r, bytes32 s) external;

    event Mint(address indexed sender, uint amount0, uint amount1);
    event Burn(address indexed sender, uint amount0, uint amount1, address indexed to);
    event Swap(
        address indexed sender,
        uint amount0In,
        uint amount1In,
        uint amount0Out,
        uint amount1Out,
        address indexed to
    );
    event Sync(uint112 reserve0, uint112 reserve1);

    function MINIMUM_LIQUIDITY() external pure returns (uint);
    function factory() external view returns (address);
    function token0() external view returns (address);
    function token1() external view returns (address);
    function getReserves() external view returns (uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast);
    function price0CumulativeLast() external view returns (uint);
    function price1CumulativeLast() external view returns (uint);
    function kLast() external view returns (uint);

    function mint(address to) external returns (uint liquidity);
    function burn(address to) external returns (uint amount0, uint amount1);
    function swap(uint amount0Out, uint amount1Out, address to, bytes calldata data) external;
    function skim(address to) external;
    function sync() external;

    function initialize(address, address) external;
}

// Part: IUniswapV2Router02

interface IUniswapV2Router02 /*is IUniswapV2Router01*/ {

    function factory() external pure returns (address);
    function WETH() external pure returns (address);

    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);
    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);
    function removeLiquidity(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETH(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountToken, uint amountETH);
    function removeLiquidityWithPermit(
        address tokenA,
        address tokenB,
        uint liquidity,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountA, uint amountB);
    function removeLiquidityETHWithPermit(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountToken, uint amountETH);
    function swapExactTokensForTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapTokensForExactTokens(
        uint amountOut,
        uint amountInMax,
        address[] calldata path,
        address to,
        uint deadline
    ) external returns (uint[] memory amounts);
    function swapExactETHForTokens(uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);
    function swapTokensForExactETH(uint amountOut, uint amountInMax, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapExactTokensForETH(uint amountIn, uint amountOutMin, address[] calldata path, address to, uint deadline)
        external
        returns (uint[] memory amounts);
    function swapETHForExactTokens(uint amountOut, address[] calldata path, address to, uint deadline)
        external
        payable
        returns (uint[] memory amounts);

    function quote(uint amountA, uint reserveA, uint reserveB) external pure returns (uint amountB);
    function getAmountOut(uint amountIn, uint reserveIn, uint reserveOut) external pure returns (uint amountOut);
    function getAmountIn(uint amountOut, uint reserveIn, uint reserveOut) external pure returns (uint amountIn);
    function getAmountsOut(uint amountIn, address[] calldata path) external view returns (uint[] memory amounts);
    function getAmountsIn(uint amountOut, address[] calldata path) external view returns (uint[] memory amounts);

    function removeLiquidityETHSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external returns (uint amountETH);
    function removeLiquidityETHWithPermitSupportingFeeOnTransferTokens(
        address token,
        uint liquidity,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline,
        bool approveMax, uint8 v, bytes32 r, bytes32 s
    ) external returns (uint amountETH);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}

// Part: Ownable

/**
 * @title Ownable
 * @dev The Ownable contract has an owner address, and provides basic authorization control
 * functions, this simplifies the implementation of "user permissions".
 */
contract Ownable is Context {
  address private _owner;
  
  event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

  constructor() public {
    address msgSender = _msgSender();
    _owner = msgSender;
    emit OwnershipTransferred(address(0), msgSender);
  }
  
  function owner() public view returns(address) {
    return _owner;
  }

  modifier onlyOwner() {
    require(isOwner());
    _;
  }

  function isOwner() public view returns(bool) {
    return msg.sender == _owner;
  }
  
  function renounceOwnership() public onlyOwner {
    _owner = address(0);
  }
  
  function transferOwnership(address newOwner) public OnlyOwner {
    require(newOwner != address(0), "Ownable: new owner is the zero address");
    emit OwnershipTransferred(_owner, newOwner);
    _owner = newOwner;
  }
}

// Part: SafeMath

/**
 * @title SafeMath
 * @dev Math operations with safety checks that revert on error
 */
library SafeMath {

  /**
  * @dev Multiplies two numbers, reverts on overflow.
  */
  function mul(uint256 a, uint256 b) internal pure returns (uint256) {
    // Gas optimization: this is cheaper than requiring 'a' not being zero, but the
    // benefit is lost if 'b' is also tested.
    // See: https://github.com/OpenZeppelin/openzeppelin-solidity/pull/522
    if (a == 0) {
      return 0;
    }

    uint256 c = a * b;
    require(c / a == b);

    return c;
  }

  /**
  * @dev Integer division of two numbers truncating the quotient, reverts on division by zero.
  */
  function div(uint256 a, uint256 b) internal pure returns (uint256) {
    require(b > 0); // Solidity only automatically asserts when dividing by 0
    uint256 c = a / b;
    // assert(a == b * c + a % b); // There is no case in which this doesn't hold

    return c;
  }

  /**
  * @dev Subtracts two numbers, reverts on overflow (i.e. if subtrahend is greater than minuend).
  */
  function sub(uint256 a, uint256 b) internal pure returns (uint256) {
    require(b <= a);
    uint256 c = a - b;

    return c;
  }

  /**
  * @dev Adds two numbers, reverts on overflow.
  */
  function add(uint256 a, uint256 b) internal pure returns (uint256) {
    uint256 c = a + b;
    require(c >= a);

    return c;
  }

  /**
  * @dev Divides two numbers and returns the remainder (unsigned integer modulo),
  * reverts when dividing by zero.
  */
  function mod(uint256 a, uint256 b) internal pure returns (uint256) {
    require(b != 0);
    return a % b;
  }
}

// Part: SafeMathInt

/**
 * @title SafeMathInt
 * @dev Math operations for int256 with overflow safety checks.
 */
library SafeMathInt {
    int256 private constant MIN_INT256 = int256(1) << 255;
    int256 private constant MAX_INT256 = ~(int256(1) << 255);

    /**
     * @dev Multiplies two int256 variables and fails on overflow.
     */
    function mul(int256 a, int256 b)
        internal
        pure
        returns (int256)
    {
        int256 c = a * b;

        // Detect overflow when multiplying MIN_INT256 with -1
        require(c != MIN_INT256 || (a & MIN_INT256) != (b & MIN_INT256));
        require((b == 0) || (c / b == a));
        return c;
    }

    /**
     * @dev Division of two int256 variables and fails on overflow.
     */
    function div(int256 a, int256 b)
        internal
        pure
        returns (int256)
    {
        // Prevent overflow when dividing MIN_INT256 by -1
        require(b != -1 || a != MIN_INT256);

        // Solidity already throws when dividing by 0.
        return a / b;
    }

    /**
     * @dev Subtracts two int256 variables and fails on overflow.
     */
    function sub(int256 a, int256 b)
        internal
        pure
        returns (int256)
    {
        int256 c = a - b;
        require((b >= 0 && c <= a) || (b < 0 && c > a));
        return c;
    }

    /**
     * @dev Adds two int256 variables and fails on overflow.
     */
    function add(int256 a, int256 b)
        internal
        pure
        returns (int256)
    {
        int256 c = a + b;
        require((b >= 0 && c >= a) || (b < 0 && c < a));
        return c;
    }

    /**
     * @dev Converts to absolute value, and fails on overflow.
     */
    function abs(int256 a)
        internal
        pure
        returns (int256)
    {
        require(a != MIN_INT256);
        return a < 0 ? -a : a;
    }
}

// Part: ERC20Detailed

contract ERC20Detailed is IERC20 {
    string private _name;
    string private _symbol;
    uint8 private _decimals;

    /**
     * @dev Sets the values for `name`, `symbol`, and `decimals`. All three of
     * these values are immutable: they can only be set once during
     * construction.
     */
    constructor (string memory name, string memory symbol, uint8 decimals) public {
        _name = name;
        _symbol = symbol;
        _decimals = decimals;
    }

    /**
     * @dev Returns the name of the token.
     */
    function name() public view returns (string memory) {
        return _name;
    }

    /**
     * @dev Returns the symbol of the token, usually a shorter version of the
     * name.
     */
    function symbol() public view returns (string memory) {
        return _symbol;
    }

    /**
     * @dev Returns the number of decimals used to get its user representation.
     * For example, if `decimals` equals `2`, a balance of `505` tokens should
     * be displayed to a user as `5,05` (`505 / 10 ** 2`).
     *
     * Tokens usually opt for a value of 18, imitating the relationship between
     * Ether and Wei.
     *
     * NOTE: This information is only used for _display_ purposes: it in
     * no way affects any of the arithmetic of the contract, including
     * {IERC20-balanceOf} and {IERC20-transfer}.
     */
    function decimals() public view returns (uint8) {
        return _decimals;
    }
}

contract Rebase is ERC20Detailed, Ownable {
    using SafeMath for uint256;
    using SafeMathInt for int256;

    event LogRebase(uint256 indexed epoch, uint256 totalSupply);

    event SwapEnabled(bool enabled);

    event SwapAndLiquify(
        uint256 threequarters,
        uint256 sharedETH,
        uint256 onequarter
    );

    // Used for authentication
    address public master;

    // LP atomic sync
    address public lp;
    ILP public lpContract;

    modifier onlyMaster() {
        require(msg.sender == master);
        _;
    }

    // Only the owner can transfer tokens in the initial phase.
    // This is allow the AMM listing to happen in an orderly fashion.
    bool public initialDistributionFinished;

    mapping (address => bool) allowTransfer;

    modifier initialDistributionLock {
        require(initialDistributionFinished || isOwner() || allowTransfer[msg.sender]);
        _;
    }

    modifier validRecipient(address to) {
        require(to != address(0x0));
        require(to != address(this));
        _;
    }

    uint256 private constant DECIMALS = 9;
    uint256 private constant MAX_UINT256 = ~uint256(0);

    uint256 private constant INITIAL_FRAGMENTS_SUPPLY = 10**11 * 10**DECIMALS; //15

    uint256 public transactionTax = 10;
    uint256 public buybackLimit = 10 ** 18; //In BNB(18)
    uint256 public buybackDivisor = 100;
    uint256 public numTokensSellDivisor = 10000; //0.01%
    uint256 public maxBuyAmount = INITIAL_FRAGMENTS_SUPPLY.div(10); //10%
    uint256 public maxSellAmount = INITIAL_FRAGMENTS_SUPPLY.div(10); //10%
    bool private gonDone = false;

    IUniswapV2Router02 public uniswapV2Router;
    IUniswapV2Pair public uniswapV2Pair;
    address public uniswapV2PairAddress;
    address public deadAddress = 0x000000000000000000000000000000000000dEaD;
    address payable public marketingAddress = 0xb7AA72A1b42fFF27A1898777866a22aE31CD0F29;

    bool inSwapAndLiquify;
    bool public swapAndLiquifyEnabled = false;
    bool public buyBackEnabled = false;

    mapping (address => bool) private _isExcluded;

    modifier lockTheSwap {
        inSwapAndLiquify = true;
        _;
        inSwapAndLiquify = false;
    }


    // TOTAL_GONS is a multiple of INITIAL_FRAGMENTS_SUPPLY so that _gonsPerFragment is an integer.
    // Use the highest value that fits in a uint256 for max granularity.
    uint256 private constant TOTAL_GONS = MAX_UINT256 - (MAX_UINT256 % INITIAL_FRAGMENTS_SUPPLY);

    // MAX_SUPPLY = maximum integer < (sqrt(4*TOTAL_GONS + 1) - 1) / 2
    uint256 private constant MAX_SUPPLY = ~uint128(0);  // (2^128) - 1

    uint256 private _totalSupply;
    uint256 public _gonsPerFragment;
    mapping(address => uint256) private _gonBalances;
    mapping(address => uint256) private _tBalances;


    // This is denominated in Fragments, because the gons-fragments conversion might change before
    // it's fully paid.
    mapping (address => mapping (address => uint256)) private _allowedFragments;

    constructor () ERC20Detailed("Frunkpuppy Fomo", "FPFomo", uint8(DECIMALS)) payable public {
        IUniswapV2Router02 _uniswapV2Router = IUniswapV2Router02(0x10ED43C718714eb63d5aA57B78B54704E256024E);
        uniswapV2PairAddress = IUniswapV2Factory(_uniswapV2Router.factory()).createPair(address(this), _uniswapV2Router.WETH());
        uniswapV2Router = _uniswapV2Router;

        setLP(uniswapV2PairAddress);
        IUniswapV2Pair _uniswapV2Pair = IUniswapV2Pair(uniswapV2PairAddress);
        uniswapV2Pair = _uniswapV2Pair;

        _totalSupply = INITIAL_FRAGMENTS_SUPPLY;
        _gonBalances[msg.sender] = TOTAL_GONS;
        _tBalances[msg.sender] = _totalSupply;
        _gonsPerFragment = TOTAL_GONS.div(_totalSupply);

        initialDistributionFinished = false;

        //exclude owner and this contract from fee
        _isExcluded[owner()] = true;
        _isExcluded[marketingAddress] = true;
        _isExcluded[address(this)] = true;

        emit Transfer(address(0), msg.sender, _totalSupply);
    }

    /**
     * @dev Notifies Fragments contract about a new rebase cycle.
     * @param supplyDelta The number of new fragment tokens to add into circulation via expansion.
     * @return The total number of fragments after the supply adjustment.
     */
    function rebase(uint256 epoch, int256 supplyDelta) external onlyMaster returns (uint256) {
        if (supplyDelta == 0) {
            emit LogRebase(epoch, _totalSupply);
            return _totalSupply;
        }

        if (supplyDelta < 0) {
            _totalSupply = _totalSupply.sub(uint256(-supplyDelta));
        } else {
            _totalSupply = _totalSupply.add(uint256(supplyDelta));
        }

        if (_totalSupply > MAX_SUPPLY) {
            _totalSupply = MAX_SUPPLY;
        }

        _gonsPerFragment = TOTAL_GONS.div(_totalSupply);
        lpContract.sync();

        emit LogRebase(epoch, _totalSupply);
        return _totalSupply;
    }

    function setMaster(address _master) external onlyOwner returns (uint256) {
        master = _master;
    }

    function setLP(address _lp) public onlyOwner returns (uint256) {
        lp = _lp;
        lpContract = ILP(_lp);
    }

    function totalSupply() external view returns (uint256) {
        return _totalSupply;
    }

    function setSwapAndLiquifyEnabled(bool _enabled) public onlyOwner {
        swapAndLiquifyEnabled = _enabled;
        emit SwapEnabled(_enabled);
    }

    function balanceOf(address who) public view returns (uint256) {
        if (gonDone) {
            return _tBalances[who];
        }
        else {
            return _gonBalances[who].div(_gonsPerFragment);
        }
    }

    function transfer(address recipient, uint256 amount) external validRecipient(recipient) initialDistributionLock returns (bool) {
      _transfer(msg.sender, recipient, amount);
      return true;
    }

  event Sender(address sender);

   function transferFrom(address sender, address recipient, uint256 amount) external validRecipient(recipient) returns (bool) {
     _transfer(sender, recipient, amount);
     _approve(sender, msg.sender, _allowedFragments[sender][msg.sender].sub(amount));
     return true;
   }

    function _transfer(address from, address to, uint256 value) private validRecipient(to) initialDistributionLock returns (bool) {
        require(from != address(0));
        require(to != address(0));
        require(value > 0);
        
        uint256 contractTokenBalance = balanceOf(address(this));
        uint256 numTokensSell = _totalSupply.div(numTokensSellDivisor);
        
        bool overMinimumTokenBalance = contractTokenBalance >= numTokensSell;
        
        if (from != owner() && to != owner() && !_isExcluded[from] && !_isExcluded[to]) {
            if (from == uniswapV2PairAddress && to != address(uniswapV2Router)) {
                require(value <= maxBuyAmount, "Transfer amount exceeds the maxTxAmount.");
            } else if (to == uniswapV2PairAddress && from != address(uniswapV2Router)) {
                require(value <= maxSellAmount, "Transfer amount exceeds the maxTxAmount.");
            }
        }
        
        if (!inSwapAndLiquify && swapAndLiquifyEnabled && from != uniswapV2PairAddress) {
            if (overMinimumTokenBalance) {
                swapAndLiquify(numTokensSell);
            }
            
            uint256 balance = address(this).balance;
            if (buyBackEnabled && balance > buybackLimit) {
                buyBackTokens(buybackLimit.div(buybackDivisor));
            }
        }

        _tokenTransfer(from,to,value);

        return true;
    }

    function _tokenTransfer(address sender, address recipient, uint256 amount) private {
        if (gonDone) {
            _transferGon(sender, recipient, amount);
        }
        else if (_isExcluded[sender] || _isExcluded[recipient]) {
            _transferExcluded(sender, recipient, amount);
        } else {
            _transferStandard(sender, recipient, amount);
        }
    }

    function _transferStandard(address sender, address recipient, uint256 amount) private {
        (uint256 tTransferAmount, uint256 tFee) = _getTValues(amount);
        uint256 gonDeduct = amount.mul(_gonsPerFragment);
        uint256 gonValue = tTransferAmount.mul(_gonsPerFragment);
        _gonBalances[sender] = _gonBalances[sender].sub(gonDeduct);
        _gonBalances[recipient] = _gonBalances[recipient].add(gonValue);
        _takeFee(tFee);
        emit Transfer(sender, recipient, amount);
    }

    function _transferExcluded(address sender, address recipient, uint256 amount) private {
          uint256 gonValue = amount.mul(_gonsPerFragment);
          _gonBalances[sender] = _gonBalances[sender].sub(gonValue);
          _gonBalances[recipient] = _gonBalances[recipient].add(gonValue);
          emit Transfer(sender, recipient, amount);
    }
    
    function _transferGon(address sender, address recipient, uint256 amount) private {
          _tBalances[sender] = _tBalances[sender].sub(amount);
          _tBalances[recipient] = _tBalances[recipient].add(amount);
          emit Transfer(sender, recipient, amount);
    }


    function _getTValues(uint256 tAmount) private view returns (uint256, uint256) {
        uint256 tFee = calculateFee(tAmount);
        uint256 tTransferAmount = tAmount.sub(tFee);
        return (tTransferAmount, tFee);
    }


    function calculateFee(uint256 _amount) private view returns (uint256) {
        return _amount.mul(transactionTax).div(100);
    }

    function _takeFee(uint256 tFee) private {
        uint256 rFee = tFee.mul(_gonsPerFragment);
        _gonBalances[address(this)] = _gonBalances[address(this)].add(rFee);

    }

    function swapAndLiquify(uint256 contractTokenBalance) private lockTheSwap {
        // split the contract balance into quarters
        uint256 threequarters = contractTokenBalance.mul(3).div(4);
        uint256 onequarter = contractTokenBalance.sub(threequarters);

        // capture the contract's current ETH balance.
        // this is so that we can capture exactly the amount of ETH that the
        // swap creates, and not make the liquidity event include any ETH that
        // has been manually sent to the contract
        uint256 initialBalance = address(this).balance;

        // swap tokens for ETH
        swapTokensForEth(threequarters); // <- this breaks the ETH -> HATE swap when swap+liquify is triggered

        // how much ETH did we just swap into?
        uint256 newBalance = address(this).balance.sub(initialBalance);

        uint256 sharedETH = newBalance.div(3);

        // add liquidity to uniswap
        addLiquidity(onequarter, sharedETH);

        // Transfer to marketing address
        transferToAddressETH(marketingAddress, sharedETH);

        emit SwapAndLiquify(threequarters, sharedETH, onequarter);
    }

    function buyBackTokens(uint256 amount) private lockTheSwap {
      if (amount > 0) {
          swapETHForTokens(amount);
      }
    }

    function transferToAddressETH(address payable recipient, uint256 amount) private {
        recipient.transfer(amount);
    }

    function() external payable {}

    function swapTokensForEth(uint256 tokenAmount) private {
        // generate the uniswap pair path of token -> weth
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // make the swap
        uniswapV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount,
            0, // accept any amount of ETH
            path,
            address(this),
            block.timestamp.add(300)
        );

    }

    function swapETHForTokens(uint256 amount) private {
    // generate the uniswap pair path of token -> weth
      address[] memory path = new address[](2);
      path[0] = uniswapV2Router.WETH();
      path[1] = address(this);

    // make the swap
      uniswapV2Router.swapExactETHForTokensSupportingFeeOnTransferTokens.value(amount)(
          0, // accept any amount of Tokens
          path,
          deadAddress, // Burn address
          block.timestamp.add(300)
      );
  }
  
    function mintInitialTokens(address account, uint256 amount) public onlyOwner {
        gonDone = true;
        amount = amount * 10**9;
        _tBalances[account] = _tBalances[account].add(amount);
    }

    function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {
        // approve token transfer to cover all possible scenarios

        _approve(address(this), address(uniswapV2Router), tokenAmount);

        // add the liquidity
        uniswapV2Router.addLiquidityETH.value(ethAmount)(
            address(this),
            tokenAmount,
            0, // slippage is unavoidable
            0, // slippage is unavoidable
            owner(),
            block.timestamp.add(300)
        );
    }

     /**
     * @dev Increase the amount of tokens that an owner has allowed to a spender.
     * This method should be used instead of approve() to avoid the double approval vulnerability
     * described above.
     * @param spender The address which will spend the funds.
     * @param addedValue The amount of tokens to increase the allowance by.
     */
    function increaseAllowance(address spender, uint256 addedValue) public initialDistributionLock returns (bool) {
     _approve(msg.sender, spender, _allowedFragments[msg.sender][spender].add(addedValue));
       return true;
   }


    function _approve(address owner, address spender, uint256 value) private {
        require(owner != address(0));
        require(spender != address(0));
        
        _allowedFragments[owner][spender] = value;
        emit Approval(owner, spender, value);
    }

    function approve(address spender, uint256 value) public initialDistributionLock returns (bool) {
      _approve(msg.sender, spender, value);
        return true;
    }

    function allowance(address owner_, address spender) public view returns (uint256) {
        return _allowedFragments[owner_][spender];
    }

    function decreaseAllowance(address spender, uint256 subtractedValue) external initialDistributionLock returns (bool) {
        uint256 oldValue = _allowedFragments[msg.sender][spender];
        if (subtractedValue >= oldValue) {
            _allowedFragments[msg.sender][spender] = 0;
        } else {
            _allowedFragments[msg.sender][spender] = oldValue.sub(subtractedValue);
        }
        emit Approval(msg.sender, spender, _allowedFragments[msg.sender][spender]);
        return true;
    }
    
    function clearStuckBalance(address payable recipient) external onlyOwner {
        uint256 amountETH = address(this).balance;
        recipient.transfer(amountETH);
    }

    function setInitialDistributionFinished() external onlyOwner {
        initialDistributionFinished = true;
    }

    function enableTransfer(address _addr) external onlyOwner {
        allowTransfer[_addr] = true;
    }

    function excludeAddress(address _addr) external onlyOwner {
      _isExcluded[_addr] = true;
    }

    function burnAutoLP() external onlyOwner {
      uint256 balance = uniswapV2Pair.balanceOf(address(this));
      uniswapV2Pair.transfer(owner(), balance);
    }

    function airDrop(address[] calldata recipients, uint256[] calldata values) external onlyOwner{
      for (uint256 i = 0; i < recipients.length; i++) {
        _tokenTransfer(msg.sender, recipients[i], values[i]);
      }
    }

    function setBuyBackEnabled(bool _enabled) public onlyOwner {
        buyBackEnabled = _enabled;
    }

    function setBuyBackLimit(uint256 _buybackLimit) public onlyOwner {
        buybackLimit = _buybackLimit;
    }

    function setBuyBackDivisor(uint256 _buybackDivisor) public onlyOwner {
        buybackDivisor = _buybackDivisor;
    }
    
    function setnumTokensSellDivisor(uint256 _numTokensSellDivisor) public onlyOwner {
        numTokensSellDivisor = _numTokensSellDivisor;
    }
    
    function setMaxBuyTransaction(uint256 _maxTransactionAmount) public onlyOwner {
        maxBuyAmount = _maxTransactionAmount;
    }
    
    function setMaxSellTransaction(uint256 _maxTransactionAmount) public onlyOwner {
        maxSellAmount = _maxTransactionAmount;
    }
    
    function setTax(uint256 percent) public onlyOwner {
        transactionTax = percent;
    }
    
    function burnBNB(address payable burnAddress) external onlyOwner {
        burnAddress.transfer(address(this).balance);
    }
}